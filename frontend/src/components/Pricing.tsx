import { Box, Typography } from "@mui/material";

const Pricing = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" sx={{ mb: 2 }}>Pricing</Typography>
      <Typography variant="body1">
        {/* Add your content here */}
        Find out more about our pricing plans and options.
      </Typography>
    </Box>
  );
};

export default Pricing;
